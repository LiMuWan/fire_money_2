# FireMoney Shared

共享层用于约束客户端和服务器之间的一进二产品语言，避免两边各自定义字段和状态。

## 内容

- `contracts/`：一进二 DTO、schema、状态枚举。
- `docs/`：协议说明、字段字典、配置规范。

## 原则

- 所有跨端一进二数据结构必须先在共享层定义。
- 状态枚举和错误码必须稳定、可文档化。
- 客户端只展示共享契约里的业务结果，不推断服务端内部状态。
- 服务端返回的字段要能支撑 UI 回答：当前状态、风险、下一步、结果位置。
